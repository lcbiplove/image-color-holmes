- name: Ensure Docker Compose is installed
  apt:
    name: docker-compose
    state: present
    update_cache: yes

- name: Pull latest Docker image with Compose
  command: docker compose pull

- name: Restart Docker Compose service
  command: docker compose up -d
